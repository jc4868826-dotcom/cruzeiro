services:
  - type: web
    name: cruzeiro-funnelos
    env: node
    plan: starter
    buildCommand: npm install && node scripts/seed-ejecutivos.js
    startCommand: node src/server.js
    disk:
      name: funnelos-data
      mountPath: /data
      sizeGB: 1
