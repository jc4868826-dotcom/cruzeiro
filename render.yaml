services:
  - type: web
    name: cruzeiro-funnelos
    env: node
    plan: starter
    buildCommand: npm install
    startCommand: node src/server.js
    disk:
      name: funnelos-data
      mountPath: /data
      sizeGB: 1
